#pragma once

#include <QDockWidget>

class MinimapWidget : public QDockWidget
{
	Q_OBJECT

public:
	explicit MinimapWidget( const QString& title, QWidget* parent = Q_NULLPTR );

private:

public slots:

private:

};